{{- if .Values.jobs.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jobs
  labels:
    app: jobs
spec:
  replicas: {{ .Values.jobs.replicas | int }}
  selector:
    matchLabels:
      app: jobs
  template:
    metadata:
      labels:
        app: jobs
    spec:
      containers:
        - name: jobs
          image: {{ .Values.jobs.image | quote }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy | quote }}
          volumeMounts:
            - name: uploads-volume
              mountPath: /app/apps/jobs/uploads
          ports:
            - containerPort: {{ .Values.jobs.port | int }}
          env:
            {{- include "common.env" . | nindent 12 }}
            - name: AUTH_SERVICE_GRPC_URL
              value: "auth-grpc.jobber.svc.cluster.local:{{ .Values.auth.port.grpc }}"
            - name: PORT
              value: "{{ .Values.jobs.port }}"
      volumes:
        - name: uploads-volume
          persistentVolumeClaim:
            claimName: uploads-pvc

{{- end }}
