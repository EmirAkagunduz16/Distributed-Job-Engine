{{- if .Values.auth.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth
  labels:
    app: auth
spec:
  replicas: {{ .Values.auth.replicas | int }}
  selector:
    matchLabels:
      app: auth
  template:
    metadata:
      labels:
        app: auth
    spec:
      initContainers:
        - name: prisma-migrate
          image: {{ .Values.auth.image | quote }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy | quote }}
          command: ["sh", "-c"]
          args:
            - |
              npx prisma migrate deploy
          env:
            {{- include "common.env" . | nindent 12 }}

      containers:
        - name: auth
          image: {{ .Values.auth.image | quote }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy | quote }}
          ports:
            - containerPort: {{ .Values.auth.port.http | int }}
            - containerPort: {{ .Values.auth.port.grpc | int }}
          env:
            {{- include "common.env" . | nindent 12 }}
            - name: PORT
              value: "{{ .Values.auth.port.http }}"
            - name: JWT_SECRET
              value: {{ .Values.auth.jwt.secret }}
            - name: JWT_EXPIRATION_MS
              value: "{{ .Values.auth.jwt.expirationMs }}"
            - name : AUTH_GRPC_SERVICE_URL
              value: "0.0.0.0:{{ .Values.auth.port.grpc }}"
{{- end }}
